{
	"name": "@showpilot/landing",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "node build.mjs",
		"preview": "node build.mjs && npx wrangler dev",
		"deploy": "node build.mjs && npx wrangler deploy"
	}
}
