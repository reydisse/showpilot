import { createFileRoute, Link, useNavigate, useRouter } from "@tanstack/react-router";
import { useMemo, useState } from "react";
import { CalendarDays, Check, ChevronDown, ChevronLeft, ChevronRight, Clock3, Copy, ExternalLink, Pencil, Plus, Send, Settings2, Trash2, UserRoundPlus, X } from "lucide-react";
import { PageSkeleton } from "@/components/ui/Skeleton";
import { useConfirmDialog } from "@/components/ui/confirm-dialog";
import { createNextService, copyCrewFromService } from "@/lib/pm-actions";
import { deleteServiceAssignment, getSchedule, remindAllServiceAssignments, remindServiceAssignment, saveScheduleProvider, saveServiceAssignment, type ScheduleProvider } from "@/lib/schedule";
import { getOrgSettings } from "@/lib/settings";
import { getTodayDateString } from "@/lib/utils";

function shiftDate(date: string, days: number) { const value = new Date(`${date}T12:00:00`); value.setDate(value.getDate() + days); return value.toISOString().slice(0, 10); }
function dayLabel(date: string) { return new Date(`${date}T12:00:00`).toLocaleDateString([], { weekday: "short", month: "short", day: "numeric" }); }
function longDate(date: string) { return new Date(`${date}T12:00:00`).toLocaleDateString([], { weekday: "long", month: "long", day: "numeric", year: "numeric" }); }
function timeLabel(value: string | null) { return value ? new Date(value).toLocaleTimeString([], { hour: "numeric", minute: "2-digit" }) : "Time not set"; }
function providerName(provider: ScheduleProvider, label = "") { return label || ({ native: "ShowPilot", "planning-center": "Planning Center", faithteams: "Faith Teams", other: "External platform" }[provider]); }
const FORM_CONTROL = "w-full rounded-xl border border-board-border bg-board-bg px-3 py-2.5 text-sm text-board-text outline-none focus:border-fire-500/50";

export const Route = createFileRoute("/$slug/schedule")({
  validateSearch: (search: Record<string, unknown>) => ({ date: typeof search.date === "string" ? search.date : undefined, assignment: typeof search.assignment === "string" ? search.assignment : undefined }),
  loaderDeps: ({ search }) => ({ date: search.date }),
  pendingComponent: PageSkeleton,
  loader: async ({ context, deps }) => {
    const { withPermission } = await import("@/lib/route-permissions");
    await withPermission(context.role, ["rundown:view", "dashboard:pm"], context.slug, context.orgId);
    const settings = await getOrgSettings({ data: { orgId: context.orgId } });
    const today = getTodayDateString(settings["org-timezone"]);
    const schedule = await getSchedule({ data: { orgId: context.orgId, from: shiftDate(today, -30), to: shiftDate(today, 90), selectedDate: deps.date } });
    const selectedDate = deps.date ?? schedule.services.find((service) => service.serviceDate >= today)?.serviceDate ?? schedule.services.at(-1)?.serviceDate ?? null;
    return { ...schedule, selectedDate, today, orgId: context.orgId, canManage: ["owner", "admin", "pm"].includes(context.role ?? "") };
  },
  component: SchedulePage,
});

type Assignment = Awaited<ReturnType<typeof getSchedule>>["assignments"][number];
type ResponseFilter = "all" | "assigned" | "confirmed" | "declined" | "open";

function SchedulePage() {
  const data = Route.useLoaderData();
  const { slug } = Route.useParams();
  const navigate = useNavigate({ from: Route.fullPath });
  const router = useRouter();
  const { confirm, ConfirmDialogEl } = useConfirmDialog();
  const [createOpen, setCreateOpen] = useState(false);
  const [assignmentOpen, setAssignmentOpen] = useState(false);
  const [editing, setEditing] = useState<Assignment | null>(null);
  const [providerOpen, setProviderOpen] = useState(false);
  const [filter, setFilter] = useState<ResponseFilter>("all");
  const [busy, setBusy] = useState(false);
  const selected = data.services.find((service) => service.serviceDate === data.selectedDate) ?? data.services[0] ?? null;
  const previous = selected ? [...data.services].reverse().find((service) => service.serviceDate < selected.serviceDate) : null;
  const assignments = useMemo(() => data.assignments.filter((assignment) => filter === "all" || (filter === "open" ? !assignment.crewMemberId : assignment.status === filter)), [data.assignments, filter]);
  const stats = { filled: data.assignments.filter((item) => item.crewMemberId).length, open: data.assignments.filter((item) => !item.crewMemberId).length, awaiting: data.assignments.filter((item) => item.status === "assigned" && item.crewMemberId).length, declined: data.assignments.filter((item) => item.status === "declined").length };
  const chooseDate = (date: string) => void navigate({ search: { date, assignment: undefined } });

  return <div className="h-full overflow-auto bg-board-bg">
    <header className="sticky top-0 z-20 border-b border-board-border bg-board-bg/95 px-4 py-3 backdrop-blur-xl md:px-6"><div className="mx-auto flex max-w-[1700px] flex-wrap items-center gap-3"><h1 className="min-w-0 flex-1 text-xl font-semibold tracking-tight text-board-text">Schedule</h1>{data.canManage ? <button onClick={() => setProviderOpen(true)} className="flex items-center gap-2 rounded-lg border border-board-border px-3 py-2 text-xs text-board-muted hover:text-board-text"><Settings2 className="h-3.5 w-3.5" />Manage source</button> : null}{data.canManage && data.provider.type === "native" ? <button onClick={() => setCreateOpen(true)} className="flex items-center gap-2 rounded-lg border border-board-border px-3 py-2 text-xs text-board-text"><Plus className="h-3.5 w-3.5" />New service</button> : null}</div></header>
    <div className="mx-auto grid min-h-[calc(100vh-58px)] max-w-[1700px] border-x border-board-border lg:grid-cols-[260px_minmax(0,1fr)]">
      <aside className="border-b border-board-border bg-board-card lg:border-b-0 lg:border-r"><MonthCalendar selectedDate={selected?.serviceDate ?? data.today} today={data.today} serviceDates={data.services.map((service) => service.serviceDate)} onSelect={chooseDate} /><div className="border-y border-board-border px-4 py-3"><div className="flex items-center justify-between"><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-board-muted">Services</p><button onClick={() => chooseDate(data.today)} className="text-[10px] text-fire-400">Today</button></div></div><div className="flex snap-x overflow-x-auto lg:block lg:max-h-[calc(100vh-430px)] lg:overflow-y-auto">{data.services.map((service) => <button key={service.serviceDate} onClick={() => chooseDate(service.serviceDate)} className={`min-w-[210px] snap-start border-b border-board-border border-l-[3px] px-4 py-4 text-left lg:w-full ${service.serviceDate === selected?.serviceDate ? "border-l-fire-500 bg-fire-500/[0.055]" : "border-l-transparent hover:bg-board-bg"}`}><p className={`text-[10px] font-semibold uppercase tracking-wider ${service.serviceDate === data.today ? "text-fire-400" : "text-board-muted"}`}>{service.serviceDate === data.today ? "Today" : dayLabel(service.serviceDate)}</p><p className="mt-2 truncate text-sm font-medium text-board-text">{service.name}</p><p className="mt-1 text-[11px] text-board-muted">{timeLabel(service.scheduledStartTime)} · {service.crewConfirmed}/{service.crewTotal} confirmed</p></button>)}</div></aside>
      <main className="min-w-0 bg-board-bg">{selected ? <><section className="border-b border-board-border bg-board-card px-4 py-5 md:px-6"><div className="flex flex-wrap items-start gap-4"><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h2 className="text-xl font-semibold text-board-text md:text-2xl">{selected.name}</h2><ChevronDown className="h-4 w-4 text-board-muted" /></div><p className="mt-2 flex flex-wrap items-center gap-x-4 gap-y-1 text-xs text-board-muted"><span className="flex items-center gap-1.5"><CalendarDays className="h-3.5 w-3.5" />{longDate(selected.serviceDate)}</span><span className="flex items-center gap-1.5"><Clock3 className="h-3.5 w-3.5" />{timeLabel(selected.scheduledStartTime)}</span></p></div>{data.provider.type === "native" && data.canManage ? <div className="flex flex-wrap gap-2"><button disabled={busy || stats.awaiting === 0} onClick={async () => { setBusy(true); try { await remindAllServiceAssignments({ data: { orgId: data.orgId, serviceDate: selected.serviceDate } }); } finally { setBusy(false); } }} className="flex items-center gap-2 rounded-lg bg-fire-500 px-3 py-2 text-xs font-semibold text-black disabled:opacity-40"><Send className="h-3.5 w-3.5" />{busy ? "Sending…" : "Send requests"}</button><button onClick={() => setAssignmentOpen(true)} className="flex items-center gap-2 rounded-lg border border-board-border px-3 py-2 text-xs text-board-text"><Plus className="h-3.5 w-3.5" />Add position</button>{previous ? <button disabled={busy || data.assignments.length > 0} onClick={async () => { setBusy(true); try { await copyCrewFromService({ data: { orgId: data.orgId, serviceDate: selected.serviceDate, copyFrom: previous.serviceDate } }); await router.invalidate(); } finally { setBusy(false); } }} className="flex items-center gap-2 rounded-lg border border-board-border px-3 py-2 text-xs text-board-text disabled:opacity-40"><Copy className="h-3.5 w-3.5" />Copy team</button> : null}</div> : data.provider.url ? <a href={data.provider.url} target="_blank" rel="noreferrer" className="flex items-center gap-2 rounded-lg bg-fire-500 px-3 py-2 text-xs font-semibold text-black">Open {providerName(data.provider.type, data.provider.label)}<ExternalLink className="h-3.5 w-3.5" /></a> : null}</div></section>
      <section className="flex flex-wrap items-center gap-x-6 gap-y-3 border-b border-board-border bg-board-card px-4 py-3 md:px-6"><Summary label="Filled" value={stats.filled} color="text-green-400" /><Summary label="Open" value={stats.open} color="text-fire-400" /><Summary label="Awaiting" value={stats.awaiting} color="text-yellow-300" /><Summary label="Declined" value={stats.declined} color="text-red-400" /><div className="ml-auto flex items-center gap-2"><select value={filter} onChange={(event) => setFilter(event.target.value as ResponseFilter)} className="rounded-lg border border-board-border bg-board-bg px-3 py-2 text-[11px] text-board-text outline-none"><option value="all">All responses</option><option value="confirmed">Confirmed</option><option value="assigned">Awaiting</option><option value="declined">Declined</option><option value="open">Open</option></select><Link to="/$slug/rundown" params={{ slug }} search={{ date: selected.serviceDate }} className="hidden items-center gap-1 rounded-lg border border-board-border px-3 py-2 text-[11px] text-board-text sm:flex">Rundown<ChevronRight className="h-3 w-3" /></Link></div></section>
      {data.provider.type !== "native" ? <div className="border-b border-fire-500/20 bg-fire-500/[0.045] px-6 py-3 text-xs text-board-muted">Assignments and confirmations are managed in <strong className="text-board-text">{providerName(data.provider.type, data.provider.label)}</strong>. ShowPilot remains the operational rundown and reporting layer.</div> : null}
      <RosterTable assignments={assignments} orgId={data.orgId} canManage={data.canManage && data.provider.type === "native"} onAdd={() => setAssignmentOpen(true)} onEdit={setEditing} onChanged={() => router.invalidate()} confirm={confirm} />
      </> : <div className="py-24 text-center"><CalendarDays className="mx-auto h-9 w-9 text-board-muted/40" /><p className="mt-4 text-sm text-board-muted">Create your first service.</p></div>}</main>
    </div>
    {createOpen ? <CreateServiceModal orgId={data.orgId} today={data.today} previousDate={data.services.at(-1)?.serviceDate} onClose={() => setCreateOpen(false)} onCreated={async (date) => { setCreateOpen(false); await router.invalidate(); chooseDate(date); }} /> : null}
    {assignmentOpen && selected ? <AssignmentModal orgId={data.orgId} serviceDate={selected.serviceDate} crew={data.crew} onClose={() => setAssignmentOpen(false)} onSaved={async () => { setAssignmentOpen(false); await router.invalidate(); }} /> : null}
    {editing ? <EditAssignmentModal orgId={data.orgId} assignment={editing} crew={data.crew} confirm={confirm} onClose={() => setEditing(null)} onSaved={async () => { setEditing(null); await router.invalidate(); }} /> : null}
    {providerOpen ? <ProviderModal orgId={data.orgId} current={data.provider} onClose={() => setProviderOpen(false)} onSaved={async () => { setProviderOpen(false); await router.invalidate(); }} /> : null}
    {ConfirmDialogEl}
  </div>;
}

function Summary({ label, value, color }: { label: string; value: number; color: string }) { return <div className="flex items-baseline gap-2 border-r border-board-border pr-6 last:border-r-0"><span className="text-[11px] text-board-muted">{label}</span><strong className={`text-sm ${color}`}>{value}</strong></div>; }
function MonthCalendar({ selectedDate, today, serviceDates, onSelect }: { selectedDate: string; today: string; serviceDates: string[]; onSelect: (date: string) => void }) { const initial = new Date(`${selectedDate}T12:00:00`); const [month, setMonth] = useState(() => new Date(initial.getFullYear(), initial.getMonth(), 1)); const serviceSet = useMemo(() => new Set(serviceDates), [serviceDates]); const startDay = month.getDay(); const daysInMonth = new Date(month.getFullYear(), month.getMonth() + 1, 0).getDate(); const cells = Array.from({ length: 42 }, (_, index) => { const day = index - startDay + 1; return day > 0 && day <= daysInMonth ? day : null; }); const dateFor = (day: number) => `${month.getFullYear()}-${String(month.getMonth() + 1).padStart(2, "0")}-${String(day).padStart(2, "0")}`; return <div className="hidden p-4 lg:block"><div className="flex items-center justify-between"><button onClick={() => setMonth((value) => new Date(value.getFullYear(), value.getMonth() - 1, 1))} aria-label="Previous month" className="rounded p-1.5 text-board-muted hover:bg-board-bg hover:text-board-text"><ChevronLeft className="h-3.5 w-3.5" /></button><p className="text-xs font-semibold text-board-text">{month.toLocaleDateString([], { month: "long", year: "numeric" })}</p><button onClick={() => setMonth((value) => new Date(value.getFullYear(), value.getMonth() + 1, 1))} aria-label="Next month" className="rounded p-1.5 text-board-muted hover:bg-board-bg hover:text-board-text"><ChevronRight className="h-3.5 w-3.5" /></button></div><div className="mt-4 grid grid-cols-7 text-center text-[9px] font-medium uppercase text-board-muted">{["S", "M", "T", "W", "T", "F", "S"].map((label, index) => <span key={`${label}-${index}`}>{label}</span>)}</div><div className="mt-2 grid grid-cols-7 gap-y-1">{cells.map((day, index) => { if (!day) return <span key={`empty-${index}`} className="h-7" />; const date = dateFor(day); const hasService = serviceSet.has(date); const selected = date === selectedDate; const isToday = date === today; return <button key={date} disabled={!hasService} onClick={() => onSelect(date)} className={`relative mx-auto flex h-7 w-7 items-center justify-center rounded-full text-[10px] transition-colors ${selected ? "bg-fire-500 font-semibold text-black" : hasService ? "text-board-text hover:bg-board-bg" : "text-board-muted/40"} ${isToday && !selected ? "ring-1 ring-fire-500/50" : ""}`}>{day}{hasService && !selected ? <span className="absolute bottom-0.5 h-0.5 w-0.5 rounded-full bg-fire-400" /> : null}</button>; })}</div><div className="mt-4 flex items-center justify-between border-t border-board-border pt-3 text-[9px] text-board-muted"><span><span className="mr-1.5 inline-block h-1.5 w-1.5 rounded-full bg-fire-400" />Service scheduled</span><button onClick={() => { const value = new Date(`${today}T12:00:00`); setMonth(new Date(value.getFullYear(), value.getMonth(), 1)); if (serviceSet.has(today)) onSelect(today); }} className="text-fire-400">This month</button></div></div>; }
function department(role: string) { const value = role.toLowerCase(); if (/audio|sound|foh|monitor|wireless|mic/.test(value)) return "Audio"; if (/camera|video|graphics|stream|projection|playback/.test(value)) return "Video"; if (/worship|vocal|guitar|bass|drum|keys|music/.test(value)) return "Worship"; return "Production"; }

function RosterTable({ assignments, orgId, canManage, onAdd, onEdit, onChanged, confirm }: { assignments: Assignment[]; orgId: string; canManage: boolean; onAdd: () => void; onEdit: (assignment: Assignment) => void; onChanged: () => Promise<void>; confirm: ReturnType<typeof useConfirmDialog>["confirm"] }) { const groups = ["Production", "Audio", "Video", "Worship"].map((name) => ({ name, rows: assignments.filter((item) => department(item.role) === name) })).filter((group) => group.rows.length); return <div className="overflow-x-auto"><div className="min-w-[900px]"><div className="grid grid-cols-[1.2fr_1fr_.9fr_.8fr_72px] border-b border-board-border bg-board-bg px-6 py-2.5 text-[10px] uppercase tracking-wider text-board-muted"><span>Position</span><span>Assigned to</span><span>Invitation</span><span>Response</span><span /></div>{groups.map((group) => <section key={group.name}><div className="border-b border-board-border bg-board-card/70 px-6 py-2 text-[10px] font-semibold uppercase tracking-[0.14em] text-board-muted">{group.name} <span className="ml-1 text-board-muted/50">{group.rows.length}</span></div>{group.rows.map((assignment) => <RosterRow key={assignment.id} assignment={assignment} orgId={orgId} canManage={canManage} onEdit={() => onEdit(assignment)} onChanged={onChanged} confirm={confirm} />)}{canManage ? <button onClick={onAdd} className="flex w-full items-center gap-2 border-b border-board-border px-6 py-2.5 text-left text-[11px] text-fire-400 hover:bg-board-card"><Plus className="h-3 w-3" />Add position</button> : null}</section>)}{!groups.length ? <div className="py-20 text-center"><UserRoundPlus className="mx-auto h-9 w-9 text-board-muted/30" /><p className="mt-3 text-sm text-board-muted">No positions scheduled.</p>{canManage ? <button onClick={onAdd} className="mt-4 rounded-lg bg-fire-500 px-4 py-2 text-xs font-semibold text-black">Add first position</button> : null}</div> : null}</div></div>; }

function RosterRow({ assignment, orgId, canManage, onEdit, onChanged, confirm }: { assignment: Assignment; orgId: string; canManage: boolean; onEdit: () => void; onChanged: () => Promise<void>; confirm: ReturnType<typeof useConfirmDialog>["confirm"] }) { const [busy, setBusy] = useState(false); const pending = assignment.status === "assigned" && assignment.crewMemberId; const statusColor = assignment.status === "confirmed" ? "text-green-400" : assignment.status === "declined" ? "text-red-400" : "text-yellow-300"; return <div className="grid grid-cols-[1.2fr_1fr_.9fr_.8fr_72px] items-center border-b border-board-border px-6 py-3 text-xs hover:bg-board-card/60"><button disabled={!canManage} onClick={onEdit} className="text-left font-medium text-board-text disabled:cursor-default">{assignment.role}</button><button disabled={!canManage} onClick={onEdit} className={`text-left disabled:cursor-default ${assignment.crewMember ? "text-board-text" : "text-fire-400"}`}>{assignment.crewMember?.name ?? "Open position"}<p className="mt-0.5 text-[9px] text-board-muted">{assignment.crewMember?.email || "Click to assign a roster contact"}</p></button><div className="text-[10px] text-board-muted">{assignment.invitedAt ? <><Send className="mr-1 inline h-3 w-3 text-green-400" />Invitation sent</> : assignment.crewMember ? "Not sent" : "—"}</div><div className={`flex items-center gap-1.5 text-[10px] capitalize ${statusColor}`}>{assignment.status === "confirmed" ? <Check className="h-3 w-3" /> : assignment.status === "declined" ? <X className="h-3 w-3" /> : <Clock3 className="h-3 w-3" />}{assignment.crewMemberId ? (assignment.status === "assigned" ? "Awaiting" : assignment.status) : "Open"}{pending && canManage ? <button disabled={busy} onClick={async () => { setBusy(true); try { await remindServiceAssignment({ data: { orgId, id: assignment.id } }); } finally { setBusy(false); } }} className="ml-2 text-[9px] text-fire-400 disabled:opacity-50">Resend</button> : null}</div><div className="flex items-center gap-1">{canManage ? <><button onClick={onEdit} aria-label={`Edit ${assignment.role}`} className="rounded p-1.5 text-board-muted hover:bg-board-border hover:text-board-text"><Pencil className="h-3.5 w-3.5" /></button><button onClick={async () => { const approved = await confirm({ title: "Remove position", description: `Remove ${assignment.role} from this service?`, confirmLabel: "Remove" }); if (!approved) return; await deleteServiceAssignment({ data: { orgId, id: assignment.id } }); await onChanged(); }} aria-label={`Remove ${assignment.role}`} className="rounded p-1.5 text-board-muted hover:bg-board-border hover:text-red-400"><Trash2 className="h-3.5 w-3.5" /></button></> : null}</div></div>; }

function CreateServiceModal({ orgId, today, previousDate, onClose, onCreated }: { orgId: string; today: string; previousDate?: string; onClose: () => void; onCreated: (date: string) => void }) { const [date, setDate] = useState(shiftDate(today, 7)); const [name, setName] = useState(""); const [time, setTime] = useState("10:00"); const [copy, setCopy] = useState(true); const [busy, setBusy] = useState(false); return <Modal title="New service" onClose={onClose}><form onSubmit={async (event) => { event.preventDefault(); setBusy(true); try { await createNextService({ data: { orgId, serviceDate: date, name, startTime: time, copyFrom: copy ? previousDate : undefined } }); onCreated(date); } finally { setBusy(false); } }} className="space-y-4"><Field label="Service name"><input value={name} onChange={(event) => setName(event.target.value)} placeholder="Sunday Morning" className={FORM_CONTROL} /></Field><div className="grid grid-cols-2 gap-3"><Field label="Date"><input type="date" value={date} onChange={(event) => setDate(event.target.value)} className={FORM_CONTROL} /></Field><Field label="Start time"><input type="time" value={time} onChange={(event) => setTime(event.target.value)} className={FORM_CONTROL} /></Field></div>{previousDate ? <label className="flex items-center gap-2 text-xs text-board-text"><input type="checkbox" checked={copy} onChange={(event) => setCopy(event.target.checked)} className="accent-fire-500" />Copy rundown from {dayLabel(previousDate)}</label> : null}<button disabled={busy || !date} className="w-full rounded-xl bg-fire-500 px-4 py-2.5 text-sm font-semibold text-black disabled:opacity-50">{busy ? "Creating…" : "Create service"}</button></form></Modal>; }
function AssignmentModal({ orgId, serviceDate, crew, onClose, onSaved }: { orgId: string; serviceDate: string; crew: Array<{ id: string; name: string; role: string; email: string }>; onClose: () => void; onSaved: () => void }) { const [role, setRole] = useState(""); const [crewId, setCrewId] = useState(""); const [notes, setNotes] = useState(""); const [busy, setBusy] = useState(false); const selectedCrew = crew.find((person) => person.id === crewId); return <Modal title="Add position" onClose={onClose}><form onSubmit={async (event) => { event.preventDefault(); setBusy(true); try { await saveServiceAssignment({ data: { orgId, serviceDate, role, crewMemberId: crewId || null, status: "assigned", notes } }); onSaved(); } finally { setBusy(false); } }} className="space-y-4"><Field label="Position"><input value={role} onChange={(event) => setRole(event.target.value)} placeholder="Camera Operator" className={FORM_CONTROL} /></Field><Field label="Roster contact"><select value={crewId} onChange={(event) => setCrewId(event.target.value)} className={FORM_CONTROL}><option value="">Leave position open</option>{crew.map((person) => <option key={person.id} value={person.id}>{person.name} · {person.role}</option>)}</select></Field><Field label="Manager note (optional)"><textarea value={notes} onChange={(event) => setNotes(event.target.value)} rows={3} maxLength={500} placeholder="Call time, arrival instructions or preparation notes" className={FORM_CONTROL} /></Field>{selectedCrew ? <div className={`rounded-lg border px-3 py-2.5 text-[11px] leading-relaxed ${selectedCrew.email ? "border-green-500/20 bg-green-500/[0.05] text-board-muted" : "border-red-500/20 bg-red-500/[0.05] text-red-300"}`}>{selectedCrew.email ? `A secure response link will be emailed to ${selectedCrew.email}. No account needed.` : "This roster contact has no email address. Add one before sending a request."}</div> : null}<button disabled={busy || !role.trim() || (Boolean(crewId) && !selectedCrew?.email)} className="w-full rounded-xl bg-fire-500 px-4 py-2.5 text-sm font-semibold text-black disabled:opacity-50">{busy ? "Scheduling…" : crewId ? "Schedule and send request" : "Add open position"}</button></form></Modal>; }
function EditAssignmentModal({ orgId, assignment, crew, confirm, onClose, onSaved }: { orgId: string; assignment: Assignment; crew: Array<{ id: string; name: string; role: string; email: string }>; confirm: ReturnType<typeof useConfirmDialog>["confirm"]; onClose: () => void; onSaved: () => void }) { const [role, setRole] = useState(assignment.role); const [crewId, setCrewId] = useState(assignment.crewMemberId ?? ""); const [notes, setNotes] = useState(assignment.notes); const [busy, setBusy] = useState(false); const selectedCrew = crew.find((person) => person.id === crewId); const personChanged = crewId !== (assignment.crewMemberId ?? ""); const submit = async () => { if (personChanged && assignment.status === "confirmed") { const approved = await confirm({ title: crewId ? "Replace confirmed crew member?" : "Make confirmed position open?", description: `${assignment.crewMember?.name ?? "This person"} has already accepted ${assignment.role}. Their confirmation will be cleared.`, confirmLabel: crewId ? "Replace person" : "Make open" }); if (!approved) return; } setBusy(true); try { await saveServiceAssignment({ data: { orgId, id: assignment.id, serviceDate: assignment.serviceDate, role, crewMemberId: crewId || null, status: assignment.status as "assigned" | "confirmed" | "declined", notes } }); onSaved(); } finally { setBusy(false); } }; return <Modal title="Edit assignment" onClose={onClose}><form onSubmit={(event) => { event.preventDefault(); void submit(); }} className="space-y-4"><div className="rounded-lg border border-board-border bg-board-bg/50 px-3 py-2.5"><p className="text-[10px] uppercase tracking-wider text-board-muted">Current response</p><p className={`mt-1 text-xs font-medium capitalize ${assignment.status === "confirmed" ? "text-green-400" : assignment.status === "declined" ? "text-red-400" : "text-yellow-300"}`}>{assignment.crewMemberId ? (assignment.status === "assigned" ? "Awaiting response" : assignment.status) : "Open position"}</p></div><Field label="Position"><input value={role} onChange={(event) => setRole(event.target.value)} className={FORM_CONTROL} /></Field><Field label="Assigned roster contact"><select value={crewId} onChange={(event) => setCrewId(event.target.value)} className={FORM_CONTROL}><option value="">Open position — nobody assigned</option>{crew.map((person) => <option key={person.id} value={person.id}>{person.name} · {person.role}</option>)}</select></Field><Field label="Manager note"><textarea value={notes} onChange={(event) => setNotes(event.target.value)} rows={3} maxLength={500} className={FORM_CONTROL} /></Field>{personChanged ? <div className={`rounded-lg border px-3 py-2.5 text-[11px] leading-relaxed ${!crewId || selectedCrew?.email ? "border-fire-500/20 bg-fire-500/[0.05] text-board-muted" : "border-red-500/20 bg-red-500/[0.05] text-red-300"}`}>{!crewId ? "The position will remain on the schedule as open. The previous person will no longer see this assignment." : selectedCrew?.email ? `This resets the response to Awaiting and sends ${selectedCrew.name} a new secure invitation.` : "This roster contact needs an email address before they can be assigned."}</div> : <p className="text-[11px] leading-relaxed text-board-muted">Changing only the position or note preserves the existing response.</p>}<div className="grid grid-cols-2 gap-3"><button type="button" onClick={onClose} className="rounded-xl border border-board-border px-4 py-2.5 text-sm text-board-text">Cancel</button><button disabled={busy || !role.trim() || (Boolean(crewId) && !selectedCrew?.email)} className="rounded-xl bg-fire-500 px-4 py-2.5 text-sm font-semibold text-black disabled:opacity-50">{busy ? "Saving…" : personChanged && crewId ? "Save and send request" : "Save changes"}</button></div></form></Modal>; }
function ProviderModal({ orgId, current, onClose, onSaved }: { orgId: string; current: { type: ScheduleProvider; url: string; label: string }; onClose: () => void; onSaved: () => void }) { const [provider, setProvider] = useState<ScheduleProvider>(current.type); const [url, setUrl] = useState(current.url); const [label, setLabel] = useState(current.label); const [busy, setBusy] = useState(false); const external = provider !== "native"; return <Modal title="Scheduling source" onClose={onClose}><form onSubmit={async (event) => { event.preventDefault(); setBusy(true); try { await saveScheduleProvider({ data: { orgId, provider, url: external ? url : "", label: provider === "other" ? label : "" } }); onSaved(); } finally { setBusy(false); } }} className="space-y-4"><Field label="Source"><select value={provider} onChange={(event) => setProvider(event.target.value as ScheduleProvider)} className={FORM_CONTROL}><option value="native">ShowPilot native</option><option value="planning-center">Planning Center</option><option value="faithteams">Faith Teams</option><option value="other">Another platform</option></select></Field>{provider === "other" ? <Field label="Platform name"><input value={label} onChange={(event) => setLabel(event.target.value)} className={FORM_CONTROL} /></Field> : null}{external ? <Field label="Scheduling workspace URL"><input type="url" value={url} onChange={(event) => setUrl(event.target.value)} className={FORM_CONTROL} /></Field> : <p className="rounded-lg border border-board-border bg-board-bg p-3 text-xs leading-relaxed text-board-muted">ShowPilot will send secure accountless invitations to roster contacts by email.</p>}<button disabled={busy || (external && !url)} className="w-full rounded-xl bg-fire-500 px-4 py-2.5 text-sm font-semibold text-black disabled:opacity-50">Save source</button></form></Modal>; }
function Modal({ title, onClose, children }: { title: string; onClose: () => void; children: React.ReactNode }) { return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4 backdrop-blur-md" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><div role="dialog" aria-modal="true" className="w-full max-w-md rounded-2xl border border-board-border bg-board-card p-5 shadow-2xl"><div className="mb-5 flex items-center justify-between"><h2 className="text-base font-semibold text-board-text">{title}</h2><button onClick={onClose} className="rounded-lg p-2 text-board-muted hover:bg-board-border"><X className="h-4 w-4" /></button></div>{children}</div></div>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-xs text-board-muted"><span className="mb-1.5 block">{label}</span>{children}</label>; }
