{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the ShowPilot Bridge window",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default", "autostart:default"]
}
